<html>
    <head>
        <% include partials/nav.ejs %>      
        <script src="/script.js"></script>  
    </head>
    <body>
        <div class="container">
        <form id = "mainForm">
                <div class="form-group">
                    <input type="text" class="form-control" id="searchSite" placeholder="Enter Site name to search...">
                    <br>
                    <button type="submit" class="btn btn-primary" name="addNewWorkSchedule">Add New</button>                    
                </div>
                <hr>
                <% console.log(siteRecords)
                     jumboID = 0 %>
                <% siteRecords.forEach(function( eachResult){ %>
                    <div class="jumbotron" name="siteRecord" id=<%= eachResult.ES_ID %></div>
                        <div class="form-group">
                            <label for="siteName">Site Name</label>
                            <input type="text" class="form-control" name="siteName" id="steName"value=<%= eachResult.S_name %>>
                        </div>
                        <div class="form-group">
                            <label for="siteAddress">Site Address</label>
                            <input type="text" class="form-control" name="siteAddress" id="siteAddress"value=<%= eachResult.S_address %>>
                        </div>
                        <button type="submit" class="btn btn-primary" name="viewWorkSchedule">View  </button>   <button type="submit" class="btn btn-primary" name="invoice">Invoice</button>
                    </div>
                <% }) %>  
        </form>
    </div>
    </body>
</html>